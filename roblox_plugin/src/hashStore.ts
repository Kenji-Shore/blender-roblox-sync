import { ServerStorage } from "@rbxts/services";

const storeName = "BlenderSyncHashStore";
// interface Data {
// 	data: buffer;
// 	users: number;
// }
// const data = new Map<string, buffer>();
// const store = ServerStorage.FindFirstChild(storeName) ?? new Instance("Folder", ServerStorage);
// store.Name = storeName;

// for (const hashData of store) {
// }
// export function setData(hash: string, data: buffer) {}

// export function getData(hash: string) {}
